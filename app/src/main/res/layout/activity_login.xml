<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_activity_coord_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingEnd="@dimen/activity_horizontal_margin"
        android:paddingStart="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context=".account.AchatAuthenticatorActivity">

        <LinearLayout
            android:id="@+id/email_login_form"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="@dimen/form_padding_principal_padding">

            <EditText
                android:id="@+id/txtUserEmail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/txt_field_defaultPadding"
                android:background="@drawable/textview_border_black"
                android:drawablePadding="16dp"
                android:drawableStart="@drawable/icon_user"
                android:focusable="true"
                android:hint="@string/prompt_email"
                android:inputType="textEmailAddress"
                android:maxLines="1"
                android:nextFocusDown="@id/txtPassword"
                android:paddingBottom="@dimen/txt_field_defaultPadding"
                android:paddingTop="@dimen/txt_field_defaultPadding"
                android:singleLine="true"
                />

            <EditText
                android:id="@+id/txtPassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/txt_field_defaultPadding"
                android:background="@drawable/textview_border_black"
                android:drawablePadding="16dp"
                android:drawableStart="@drawable/icon_locked"
                android:focusable="true"
                android:hint="@string/prompt_password"
                android:imeActionId="@+id/btnLogin"
                android:imeActionLabel="@string/action_sign_in_short"
                android:imeOptions="actionGo"
                android:inputType="textPassword"
                android:nextFocusDown="@id/btnLogin"
                android:nextFocusUp="@id/txtUserEmail"
                android:paddingBottom="@dimen/txt_field_defaultPadding"
                android:paddingTop="@dimen/txt_field_defaultPadding"
                android:singleLine="true"
                />

            <Button
                android:id="@+id/btnLogin"
                style="@style/Widget.AppCompat.Button.Colored"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/btn_login_margin"
                android:layout_marginTop="@dimen/btn_login_margin"
                android:contentDescription="@string/cd_sign_in"
                android:focusable="true"
                android:nextFocusDown="@id/btn_linktoReg"
                android:padding="@dimen/form_button_padding"
                android:text="@string/action_sign_in_short"/>

            <Button
                android:id="@+id/btn_linktoReg"
                style="@style/Widget.AppCompat.Button.Borderless"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:contentDescription="@string/cd_btn_link_to_registration"
                android:focusable="true"
                android:nextFocusUp="@id/btnLogin"
                android:text="@string/btn_link_to_register"
                android:textAllCaps="false"
                android:textColor="@color/primary_text"
                android:textSize="@dimen/txt_link_to_login_size"
                android:textStyle="bold"/>

        </LinearLayout>
    </LinearLayout>
</android.support.design.widget.CoordinatorLayout>

